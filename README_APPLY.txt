PE-ON V10 교과세특 전용 CSS 수정본

적용 파일
- src/components/StudentRecords.jsx
- src/styles/StudentRecords.css

적용 방법
1. 압축을 풉니다.
2. PE-ON-V10 폴더에 src 폴더를 그대로 덮어씁니다.
3. 터미널에서 npm run build 실행
4. 정상 빌드 후 firebase deploy --only hosting 실행

검증
- StudentRecords.jsx JSX 문법 검사 완료
- StudentRecords.css 중괄호 구조 검사 완료
- PC / 태블릿 / 모바일 전용 반응형 규칙 분리
