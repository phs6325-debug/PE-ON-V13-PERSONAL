PE-ON V13.7 공통 조회 설정 복구

추가 기능
- 학년도 / 학기 / 학년 선택
- 조회: 선택한 설정을 현재 적용값으로 반영
- 저장: 선택값을 로컬 및 로그인 계정 Firestore에 저장
- 불러오기: 저장된 선택값을 다시 불러와 적용
- 현재 설정 유지: 다음 접속 시 자동 적용되는 기본값으로 저장
- 현재 적용 설정과 처리 결과 메시지 표시

배포
npm run build
firebase deploy --only hosting
